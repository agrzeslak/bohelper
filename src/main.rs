use std::process;

fn main() {
    // let pattern = bohelper::generate_pattern(12500000);
    // println!("{}", pattern);

    // let offset =
    // bohelper::get_pattern_offset("42397042", bohelper::generate_pattern(3000).as_str())
    // .unwrap();

    // println!("{}", offset);

    // bohelper::run().unwrap_or_else(|err| {
    //     eprintln!("Error encountered: {}", err);
    //     process::exit(1);
    // });
}
